# دومنو الصحبة v6.2 — Game Feel & PWA

هذه النسخة مبنية على v5 وتحافظ على Firebase الحالي والشات الكتابي والصوتي.

## الجديد
- ترتيب اليد: ذكي / الدبلات أولًا / حسب الرقم / يدوي بالسحب / بدون ترتيب.
- إبراز طرفي سلسلة الدومينو أثناء دورك.
- Highlight + تركيز تلقائي على آخر حجر تم لعبه.
- صوت مزدوج للدبل + اهتزاز خفيف على الأجهزة التي تدعم Vibration.
- شاشة نتيجة بين الجولات مع النتيجة والتقدم نحو 51/101/151/201.
- ملخص نهائي: عدد الجولات، مرات القفل، أكبر جولة.
- سجل آخر 10 مباريات على الجهاز فقط باستخدام localStorage.
- قواعد إضافية قبل إنشاء الغرفة:
  - البداية: أعلى دبل / منشئ الغرفة / عشوائي.
  - حساب القفل: فرق الأحجار / مجموع أحجار الخاسر.
- مؤشر إعادة الاتصال، واستمرار المباراة عند عودة الشبكة.
- تنظيف الغرفة المحفوظة القديمة بعد 7 أيام عند فتحها من نفس الجهاز/الحساب المجهول.
- PWA حقيقية: Service Worker + أيقونات 192/512 + زر تثبيت عندما يدعم المتصفح ذلك.
- ثيمان للطاولة: أخضر كلاسيكي وخشبي داكن.
- نفس Footer: تصميم وبرمجة محمد حسين — iegy.net.

## الرفع
استبدل محتويات الريبو الحالية بكل محتويات ZIP، بما في ذلك:
- `service-worker.js`
- مجلد `icons/`
- مجلد `assets/`

لا تحتاج لتغيير `firebase-config.js` أو Realtime Database Rules الحالية.

## ملاحظة التنظيف التلقائي
لأن المشروع GitHub Pages + Firebase بدون Backend أو Cron، لا يمكن إجراء حذف عالمي دوري لكل الغرف. v6 تنظف الغرفة القديمة التي تخص الجهاز الحالي عندما يرجع المستخدم إليها، وهو حل آمن يحافظ على التشغيل المجاني 100%.


## v6.1 hotfix
- إصلاح اتجاه أنصاف الأحجار عند التفاف مسار الـSnake من اليمين إلى اليسار.
- ضبط مؤشر نهايتي اللعب حسب اتجاه المسار الفعلي.
- لا يوجد أي تغيير في Firebase أو قواعد اللعب.


## v6.2
- إضافة تذكير لطيف: «اللعب للمتعة، فلا تنسَ ذكر الله وأداء الفرائض في أوقاتها.» في أسفل الموقع.
- يظهر التذكير مرة واحدة فقط عند بداية أول جولة في كل مباراة، ويختفي تلقائيًا.


## v6.3
- تصميم أحجار دومينو أكثر واقعية: خامة عاجية، حواف بارزة، نقاط غائرة وSpinner في المنتصف.
- أصوات جديدة مولدة خصيصًا لخبطة الحجر، الدبل، والسحب.
- قاعدة جديدة افتراضية: أول جولة يبدأها أعلى دبل/مجموع، وبعدها الفائز بالجولة السابقة يبدأ الجولة التالية.
- الشات الصوتي أصبح TURN-ready. على بيانات الهاتف (CGNAT) تحتاج TURN؛ ضع بيانات Open Relay المجانية في `turn-config.js`.

### تفعيل الشات الصوتي على بيانات الهاتف 4G/5G
الإصدار v6.3 يدعم TURN تلقائيًا عند وضع بيانات Open Relay / Metered في `turn-config.js`:

```js
export const turnConfig = {
  appName: "اسم تطبيق Metered",
  apiKey: "API_KEY",
  credentialsUrl: ""
};
```

بدون هذه البيانات سيستخدم WebRTC خوادم STUN فقط، وهذا قد يعمل على Wi‑Fi ويفشل على بعض شبكات الهاتف بسبب CGNAT/NAT الصارم.


## v6.3.1 — TURN على 4G/5G
- تم تفعيل Metered TURN مباشرة داخل `turn-config.js`.
- WebRTC يجرب الاتصال المباشر أولًا ثم يستخدم TURN تلقائيًا عند الحاجة بسبب CGNAT أو قيود شبكة المحمول.
- يدعم UDP/TCP على المنفذ 80 وTURN/TLS على 443.
- تنبيه: الموقع Static، لذلك TURN credential ظاهر في JavaScript للزائر. راقب الاستهلاك وغيّر الـcredential إذا ظهر استخدام غير معتاد.
